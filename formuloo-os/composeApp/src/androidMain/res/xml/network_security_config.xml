<?xml version="1.0" encoding="utf-8"?>
<!--
    Aucun backend de production n'existe encore (BASE_URL pointe sur le backend
    de développement local). Le trafic en clair (HTTP) n'est autorisé que vers
    cet hôte de dev — à restreindre à un network security config "debug-only"
    le jour où une vraie API de production en HTTPS sera disponible.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
